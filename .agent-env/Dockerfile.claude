FROM docker.io/library/node:20-slim

RUN apt-get update && apt-get install -y \
    git \
    curl \
    make \
    && rm -rf /var/lib/apt/lists/*

RUN npm install -g @anthropic-ai/claude-code

WORKDIR /projects/expenses-form

CMD ["claude"]
